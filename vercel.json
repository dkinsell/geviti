{
  "version": 2,
  "buildCommand": "prisma generate && prisma db push && npm run pre-build-train && npm run build",
  "installCommand": "npm install",
  "framework": "nextjs",
  "outputDirectory": ".next",
  "regions": ["iad1"],
  "env": {
    "DATABASE_URL": "file:./dev.db",
    "ML_MODEL_PATH": "./public/model/model.json"
  }
}
